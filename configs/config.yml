run_dirs:
  - flexible_self_docking # this specifies which equibind model to use. can try train on both?

inference_path: 'data/new' # this should be your input file path as described in the main readme

output_directory: 'hidden_layers/receptor/new' # the hidden layers will be saved here

batch_size: 100

layer: 'receptor' # which layer do you want (ligand or receptor)